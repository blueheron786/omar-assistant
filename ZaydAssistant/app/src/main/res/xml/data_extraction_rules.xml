<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude sensitive data from cloud backup -->
        <exclude domain="sharedpref" path="zayd_secure_prefs.xml" />
        <exclude domain="database" />
    </cloud-backup>
    <device-transfer>
        <!-- Allow non-sensitive data for device transfer -->
        <include domain="file" />
        <exclude domain="sharedpref" path="zayd_secure_prefs.xml" />
    </device-transfer>
</data-extraction-rules>
